[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: hits a real external API (Gemini/Groq); deselect with -m "not live"
filterwarnings =
    ignore::DeprecationWarning
